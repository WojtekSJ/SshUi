version: '3.8'

services:
  ollama-ssh-ui:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
    container_name: ollama-ssh-ui 